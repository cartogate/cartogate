"""usersvc — a tiny user/auth service used as the Windsurf A/B duplication-bait codebase.

The reusable top-level symbols an agent should *find and reuse* (not re-implement) are
``User`` (models), ``authenticate`` and ``validate`` (auth). See ../SYMBOLS.md.
"""
