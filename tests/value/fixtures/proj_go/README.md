# proj_go

A tiny Go module used as an illustrative fixture for the value study.

The `Authenticate` function is the public entry point — it checks a name before allowing access.
See the `auth` package for details.
