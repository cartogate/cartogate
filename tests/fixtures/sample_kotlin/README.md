# sample_kotlin

A tiny Kotlin project used by Cartogate's extraction tests.

`AuthService.authenticate` verifies a user. The docs reference `authenticate` so `doc_drift`
has a documented symbol to flag.
