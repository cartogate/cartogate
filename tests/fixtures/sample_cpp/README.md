# sample_cpp

A tiny C++ project used by Cartogate's extraction tests.

`User::isActive` reports whether a user is valid. The docs reference `isActive` so `doc_drift`
has a documented symbol to flag.
