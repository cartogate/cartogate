# sample_csharp

A tiny C# project used by Cartogate's extraction tests.

`AuthService` exposes `Authenticate` to verify a caller's identity. The docs reference
`Authenticate` so `doc_drift` has a documented symbol to flag.
